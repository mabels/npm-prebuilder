"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fs = require("fs");
const path = require("path");
function findPathOfPackageJson(str) {
    if (str.length === 0) {
        return null;
    }
    const stat = fs.statSync(str);
    if (!stat) {
        throw new Error(`this must be somewhere ${str}`);
    }
    let pjson;
    let base;
    if (stat.isDirectory()) {
        base = str;
        pjson = path.join(base, 'package.json');
    }
    else {
        base = path.dirname(str);
        pjson = path.join(base, 'package.json');
    }
    const ret = fs.existsSync(pjson);
    if (!ret) {
        if (base != path.dirname(base)) {
            return findPathOfPackageJson(path.dirname(base));
        }
        else {
            return null;
        }
    }
    return base;
}
exports.findPathOfPackageJson = findPathOfPackageJson;
//# sourceMappingURL=find-path-to-package-json.js.map