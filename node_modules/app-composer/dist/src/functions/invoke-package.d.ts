import { InvokePackage } from '../types/invoke-package';
import { EntryPoint } from '../types/entry-point';
export declare function invokePackage(basePath: string, packageName: string, entryPoints: EntryPoint[]): InvokePackage;
