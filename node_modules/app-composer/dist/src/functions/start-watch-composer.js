"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const path = require("path");
const chokidar = require("chokidar");
const watcher_1 = require("../types/watcher");
function startWatchComposer(baseDir = __dirname) {
    const watchDir = path.join(baseDir, 'compose');
    const watcher = new watcher_1.Watcher(path.basename(baseDir), baseDir, watchDir);
    console.log(`composer starts in ${baseDir} watches: ${watchDir}`);
    const choki = chokidar.watch(watchDir, {
        ignoreInitial: true
    });
    choki.on('all', () => watcher.dog('watch'));
    choki.on('ready', () => watcher.dog('ready'));
}
exports.startWatchComposer = startWatchComposer;
//# sourceMappingURL=start-watch-composer.js.map