"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const path = require("path");
const fs = require("fs");
const invocation_1 = require("../types/invocation");
function writeComposedJs(pkgName, directory, names) {
    const composedJs = path.join(directory, 'index.js');
    const invocation = new invocation_1.Invocation(pkgName);
    names.filter((n) => !!n.invocationJson).map((n) => {
        invocation.merge(invocation_1.Invocation.fill(n.invocationJson));
    });
    fs.writeFileSync(composedJs, invocation.build(invocation.jsGlobalRequires));
    return composedJs;
}
exports.writeComposedJs = writeComposedJs;
//# sourceMappingURL=write-composed-js.js.map