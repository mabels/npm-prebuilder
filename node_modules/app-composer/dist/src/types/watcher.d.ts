import * as execa from 'execa';
import { WatcherState } from './watcher-state';
export declare class Watcher {
    prevPkgs: string[];
    started?: execa.ExecaChildProcess;
    watcherState: WatcherState;
    watcherSrc: string;
    readonly watchDir: string;
    readonly baseDir: string;
    readonly pkgName: string;
    constructor(pkgName: string, baseDir: string, watchDir: string);
    private run;
    private restartComposeJs;
    restartDog(src: string): void;
    dog(src: string): void;
}
