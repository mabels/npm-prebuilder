'use strict';

Object.defineProperty(exports, "__esModule", {
	value: true
});
exports.default = getForcedCaseFn;

var _getForcedCase = require('./get-forced-case');

var _getForcedCase2 = _interopRequireDefault(_getForcedCase);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * Get forced case for rule
 * @param {object} rule to parse
 * @return {fn} transform function applying the enforced case
 */
function getForcedCaseFn(rule) {
	var noop = function noop(input) {
		return input;
	};
	var lowerCase = function lowerCase(input) {
		return String.prototype.toLowerCase.call(input);
	};
	var upperCase = function upperCase(input) {
		return String.prototype.toUpperCase.call(input);
	};

	if (!rule) {
		return noop;
	}

	var forcedCase = (0, _getForcedCase2.default)(rule);

	if (forcedCase === null) {
		return noop;
	}

	return forcedCase === 'lowerCase' ? lowerCase : upperCase;
}
module.exports = exports['default'];