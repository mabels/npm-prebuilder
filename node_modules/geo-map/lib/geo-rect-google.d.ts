/// <reference types="googlemaps" />
import * as Types from './types';
export declare class GeoRectGoogle implements Types.GeoRectImplementation {
    private api;
    private bounds;
    static create(data: Types.GeoBounds, context: Types.RectContext): GeoRectGoogle;
    static from(bounds: google.maps.LatLngBounds, context: Types.RectContext): GeoRectGoogle;
    private constructor();
    getBounds(): Promise<Types.GeoBounds>;
    toBounds(): google.maps.LatLngBounds;
    coversLocation(point: Types.GeoPoint): Promise<boolean>;
}
