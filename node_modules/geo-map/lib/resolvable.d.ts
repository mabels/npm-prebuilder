export declare class Resolvable<T> implements PromiseLike<T> {
    private promise;
    private internalResolve;
    private internalReject;
    constructor();
    resolve(value: T): void;
    reject(error: Error): void;
    then<V>(fn: (value: T) => V | PromiseLike<V>): PromiseLike<V>;
}
