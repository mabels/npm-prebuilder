/// <reference types="heremaps" />
import * as Types from './types';
export declare class GeoRectHere implements Types.GeoRectImplementation {
    private api;
    private rect;
    static create(data: Types.GeoBounds, context: Types.RectContext): GeoRectHere;
    static from(rect: H.geo.Rect, context: Types.RectContext): GeoRectHere;
    private constructor();
    coversLocation(point: Types.GeoPoint): Promise<boolean>;
    getBounds(): Promise<Types.GeoBounds>;
    toRect(): H.geo.Rect;
}
