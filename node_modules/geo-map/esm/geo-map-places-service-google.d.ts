import * as Types from './types';
export declare class GeoMapPlacesServiceGoogle implements Types.GeoMapPlacesServiceImplementation {
    private readonly api;
    static create(init: {
        api: Types.GoogleApi;
    }): GeoMapPlacesServiceGoogle;
    private constructor();
    get(placeId: string): Promise<Types.Result<Types.GeoMapPlaceDetails>>;
    search(needle: string, center: Types.GeoPoint, radius: number): Promise<Types.Result<Types.GeoMapPlace[]>>;
    private convertResult;
    distanceBetween(from: Types.GeoPoint, to: Types.GeoPoint, radius?: number): number;
}
