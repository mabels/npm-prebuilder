import * as Types from './types';
import { GeoMapDirectionResult } from './types';
export declare class GeoMapDirectionServiceGoogle implements Types.GeoMapDirectionServiceImplementation {
    private readonly implementation;
    private readonly api;
    private lines;
    static create(init: {
        api: Types.GoogleApi;
        map: Types.GeoMapImplementation;
    }): GeoMapDirectionServiceGoogle;
    private constructor();
    paintRoute(from: Types.GeoPoint, to: Types.GeoPoint): Promise<GeoMapDirectionResult>;
    clear(): Promise<void>;
}
