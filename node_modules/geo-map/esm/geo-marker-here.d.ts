import * as Types from './types';
export declare class GeoMarkerHere implements Types.GeoMarkerImplementation {
    private implementation;
    private iconMarkup;
    private icon;
    private marker;
    private anchor;
    static create(config: Types.GeoMarkerConfig, context: Types.HereMarkerContext): GeoMarkerHere;
    private constructor();
    getIcon(): Promise<string>;
    setIcon(icon: string): Promise<Types.Result<void>>;
    setPosition(position: Types.GeoPoint): Promise<void>;
    getPosition(): Promise<Types.GeoPoint>;
    remove(): Promise<void>;
}
