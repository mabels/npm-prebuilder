import * as Types from './types';
export declare class GeoMarkerGoogle implements Types.GeoMarkerImplementation {
    private readonly implementation;
    private iconMarkup;
    private marker;
    private anchor;
    static create(config: Types.GeoMarkerConfig, context: Types.GoogleMarkerContext): GeoMarkerGoogle;
    private constructor();
    getIcon(): Promise<string>;
    setIcon(icon: string): Promise<Types.Result<void>>;
    setPosition(position: Types.GeoPoint): Promise<void>;
    getPosition(): Promise<Types.GeoPoint>;
    remove(): Promise<void>;
}
