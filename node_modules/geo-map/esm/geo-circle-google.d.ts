import * as Types from './types';
export declare class GeoCircleGoogle implements Types.GeoCircleImplementation {
    private circle;
    static create(config: Types.GeoCircleConfig, context: Types.CircleContext): GeoCircleGoogle;
    private constructor();
    getBounds(): Promise<Types.GeoBounds>;
}
