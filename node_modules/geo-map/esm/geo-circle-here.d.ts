import * as Types from './types';
export declare class GeoCircleHere implements Types.GeoCircleImplementation {
    private api;
    private circle;
    static create(config: Types.GeoCircleConfig, context: Types.CircleContext): GeoCircleHere;
    private constructor();
    getBounds(): Promise<Types.GeoBounds>;
}
